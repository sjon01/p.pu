function update_booking_image(e, i) {
    console.log("Changing booking image"), $("#booking_image_" + i).attr("src", $(e).find(":selected").data("filename")).parent().show()
}
$(window).on("scroll touchmove", function() {
    $("#navbar-fixed").toggleClass("tiny", 0 < $(document).scrollTop()), $(".dropdown-menu").toggleClass("tiny", 0 < $(document).scrollTop()), $("main").toggleClass("tiny", 0 < $(document).scrollTop())
}), $(".readmore").click(function() {
    var e = $(this);
    e.toggleClass("expanded"), e.hasClass("expanded") ? e.html("Show less") : e.html("Read more")
}), $(".rating input:radio").attr("checked", !1), $(".rating input").click(function() {
    $(".rating span").removeClass("checked"), $(this).parent().addClass("checked")
}), $("input:radio").change(function() {
    var e = this.value;
    $("#review-rating").attr("value", e)
}), $(document).ready(function() {
    var e; - 1 == document.cookie.indexOf("cookies=true") && ((e = $('<div class="cookies"><span>By using this website you are consenting to us using cookies to enhance your experience</span><div><span class="a">OK</span></div></div>')).find(".a").click(function() {
        e.hide(), document.cookie = "cookies=true;"
    }), $("body").append(e)), $("#reviews-form").on("submit", function() {
        var s = $(this);
        return $.post($(this).attr("action"), $(this).serialize() + "&btn_post_review=", function(e) {
            console.log(e);
            e = $.parseJSON(e);
            if (0 < Object.keys(e.errors).length)
                for (var i in s.find(":input").each(function() {
                        $(this).attr("name") in e.errors ? $(this).addClass("error") : $(this).removeClass("error")
                    }), e.errors) {
                    s.find("#message").addClass("error").html(e.errors[i]);
                    break
                } else {
                    for (var i in s.find(":input").each(function() {
                            $(this).removeClass("error")
                        }), e.values) s.find("[name=" + i + "]").val(e.values[i]);
                    s.find("#message").removeClass("error").addClass("success").html("Your review has been submitted!")
                }
        }), !1
    }), $(".quick-search__title").click(function(e) {
        $(this).hasClass("quick-search__title--clicked") ? ($(this).removeClass("quick-search__title--clicked"), $(this).parent().find(".quick-search__dropdown").slideUp()) : ($(".quick-search__dropdown").slideUp(), $(".quick-search__title").removeClass("quick-search__title--clicked"), $(this).addClass("quick-search__title--clicked"), $(this).parent().find(".quick-search__dropdown").slideDown(), e.stopPropagation())
    }), $(document).click(function() {
        $(".quick-search__dropdown").slideUp(), $(".quick-search__title--clicked").each(function() {
            $(this).removeClass("quick-title--clicked")
        })
    }), $(".quick-search__dropdown a").click(function(e) {
        return e.stopPropagation(), !0
    })
});